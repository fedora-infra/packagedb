# VCS ACLs
# avail|@groups,users|rpms/Package/branch

#for pkg in sorted(packageAcls.keys())
  #for branch in sorted(packageAcls[pkg].keys())
    #with inAclGroup = ','.join(map(lambda g: '@' + g, packageAcls[pkg][branch]['commit'].groups))
      #with inAclPeople = ','.join(packageAcls[pkg][branch]['commit'].people)
        #choose
          #when inAclPeople and inAclGroup
avail | ${','.join((inAclGroup,inAclPeople))} | rpms/${pkg}/${branch}
          #end
          #otherwise
avail | ${inAclGroup}${inAclPeople} | rpms/${pkg}/${branch}
          #end 
        #end
      #end
    #end
  #end
#end
